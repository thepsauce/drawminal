#include <math.h>
#include <stdio.h>

#include "matrix.h"

int main() {
	// Matrix *m = matDiag(3, 3.0, 3.0, 3.0);
	// Vec2 axis = (Vec2){1, 1};
	// Matrix *nm = rotateCoordMatrix(m, axis, 3.14159, 0);
	// for (size_t i = 0; i < m->cols * m->rows; i++) {
	// 	printf("%f\n", nm->elements[i]);
	// }
	return 0;
}
